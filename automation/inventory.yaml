---
all:
  vars:
    ansible_user: root
    ansible_ssh_private_key_file: ~/.ssh/robot-hackathon

robots:
  hosts:
    data.robot.lan:
      team: team-1
    ultron.robot.lan:
    terminator.robot.lan:
    abcwarrior.robot.lan:

berlin:
  hosts:
    terminator.robot.lan:
    abcwarrior.robot.lan:

munich:
  hosts:
    data.robot.lan:
    ultron.robot.lan:
