---
ansible-navigator:
  execution-environment:
    image: quay.io/sifa/devsecops-workshop:devel
    pull:
      policy: always
  logging:
    level: critical
  mode: stdout
  playbook-artifact:
    enable: true
    save-as: /tmp/devsecops-{playbook_name}-artifact-{time_stamp}.json
