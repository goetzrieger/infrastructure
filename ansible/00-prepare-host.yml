---
- hosts: rhpdsclusters
  vars_files:
  - ../vars.yml
  tasks:
  - import_role:
      name: prepare-host
      tasks_from: prepare-host.yml
  - import_role:
      name: prepare-host
      tasks_from: install-openshift-client.yml
    delegate_to: localhost
  tags:
    - prepare
